import { createTheme } from '@mui/material';

export const mainTheme = createTheme({
    palette: {
        background: {},
        primary: {
            main: '#6200ee',
        },
    },
    typography: {
        fontFamily: 'Arial',
        h4: {
            color: '#6200ee',
        },
    },
});
