import { createTheme } from '@mui/material';

export const mainTheme = createTheme({
    palette: {
        background: {},
        primary: {
            main: '#6200ee',
        },
    },
    typography: {
        h4: {
            color: '#6200ee',
        },
    },
});
